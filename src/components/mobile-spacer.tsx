/**
 * Phase 3.5 — Mobile Spacer
 * Add padding at bottom on mobile to prevent content hidden by bottom nav
 */
export function MobileSpacer() {
  return <div className="h-16 md:h-0" aria-hidden="true" />;
}
