# SECURITY FINDINGS — OF Manager (fork commercial)

> Registre des vulnérabilités **confirmées** (constats bruts passés par une **vérification adversariale** indépendante : 1 sceptique/finding chargé de réfuter). Un constat de l'audit initial (fuite T3P vers ENTREPRISE) a été **RÉFUTÉ** et retiré. Date : 2026-08-26.
> CVSS = estimation v3.1 de l'auditeur. Priorité : P0 (immédiat) · P1 (très urgent) · P2 (important) · P3 (amélioration).

## Synthèse par gravité

| Gravité | Nombre |
|---|---|
| 🔴 CRITIQUE | 1 |
| 🟠 ÉLEVÉE | 4 |
| 🟡 MOYENNE | 13 |
| 🟢 FAIBLE | 10 |
| 🔵 INFORMATION | 4 |

## Tableau des vulnérabilités

| ID | Vulnérabilité | Gravité | CVSS | Impact | Exploitabilité | Preuve (fichier:ligne) | Correction | Priorité |
|---|---|---|---|---|---|---|---|---|
| F-01 (SEC-79) | **Escalade inter-tenant via le trigger d'impersonation non gardé** | 🔴 CRITIQUE | ~9.6 | Prise de contrôle de **n'importe quel organisme** (accès total ADMIN à toutes les données d'un tenant arbitraire) + escalade verticale | **Élevée** : tout compte connecté (même APPRENANT/FORMATEUR) via un POST sur `/api/auth/session` ; CSRF inopérant (l'attaquant agit sur sa propre session) | `src/auth.config.ts:129-137` (callback jwt, branche `trigger==="update"` sans contrôle `token.role`) ; `src/lib/impersonation.ts:46-61` ; garde présente seulement dans `src/lib/actions/impersonation-actions.ts:21` (non empruntée par le POST direct) | Vérifier `token.role === "SUPERADMIN"` **dans le callback jwt** avant `applyImpersonationStart` | **P0** |
| F-02 (SEC-26) | **Stored XSS dans le contenu des leçons e-learning, inscriptible par un compte à faible privilège (BFLA)** | 🟠 ÉLEVÉE | ~7.3 | Exécution de JS dans le navigateur d'**autres apprenants ET du staff/admin** consultant la leçon → vol de session, actions au nom de la victime | Élevée intra-tenant : `updateLecon` via `getTenantDb()` accepte APPRENANT/FORMATEUR (BFLA « P2-1 » documenté) ; contenu écrit brut, rendu en `dangerouslySetInnerHTML` ; CSP `unsafe-inline` n'arrête pas `<img onerror>` | Écriture : `src/lib/actions/cours-actions.ts:181-189` ; rendu : `src/app/(app)/mes-cours/[coursId]/page.tsx:212-216` ; garde manquante : `src/lib/tenant.ts:156-179` | Gater les mutations e-learning par `requireStaffTenant` **+** assainir le HTML (allowlist) à l'écriture/lecture | **P1** |
| F-03 (SEC-14) | **Révocation de session incomplète** (session unique / reset / logout contournables) | 🟠 ÉLEVÉE | ~5.4 | Un JWT censé révoqué (reset admin, reprise sur autre appareil, désactivation d'un non-admin) reste exploitable ≤12 h sur **toutes les Server Actions et routes API** ; réponse à incident cassée | Moyenne : nécessite un cookie de session valide (non forgeable) ; pas d'escalade ni de fuite cross-tenant | `src/lib/tenant.ts:147-179` (guards sans contrôle `sid`) ; `src/app/(app)/layout.tsx:69` + `espace-entreprise/layout.tsx:37` (seuls points de contrôle) ; `src/lib/actions/account-actions.ts:63-67` (changePassword ne rote pas `activeSessionId`) ; `src/app/deconnexion/route.ts` (signOut ne purge pas) | Contrôle `sid == activeSessionId` (+ `isActive`) dans les guards centraux ; rotation sur changePassword ; purge au logout | **P1** |
| F-04 (SEC-50/9/10) | **Rate-limiting contournable en serverless sans Upstash** | 🟠 ÉLEVÉE | ~6.5 | Brute-force login, flood lead/demo/verification, abus IA/coût : toutes les protections `checkLimit` deviennent contournables | **Conditionnelle à la config prod** : si `UPSTASH_*` non défini sur Vercel → compteur mémoire par-instance (non partagé, réinit cold-start) ; requêtes parallèles réparties sur N instances | `src/lib/rate-limit.ts:15,42,47-53,62-65` (repli mémoire, simple `console.error`) ; `.env:34-35` (UPSTASH vide) ; aucun WAF/edge/middleware compensatoire | Rendre Upstash **obligatoire** (fail-closed au boot) en prod ; + CAPTCHA login progressif | **P1** |
| F-05 (SEC-52) | **Couche d'auth reposant sur `next-auth` beta non-GA** | 🟠 ÉLEVÉE | — | Toute l'authentification d'un SaaS commercial dépend d'une pré-release (API instables, patchs non garantis) | Risque de supply-chain/maintenance, pas d'exploit direct | `package.json:45` `next-auth ^5.0.0-beta.32` ; `overrides @auth/core 0.41.3` | Plan de passage next-auth v5 GA ; surveillance advisories `@auth/core` | **P2** |
| F-06 (SEC-23+24) | **Aucune défense en profondeur base : RLS inerte + connexion en rôle `owner`** | 🟡 MOYENNE | — | Si une faille de scoping applicatif survient (ex. F-01), **aucune barrière base** ne contient la fuite cross-tenant | Non exploitable seule (défense en profondeur manquante) | `src/lib/prisma.ts:39,43,83,99` (flags OFF) ; `prisma/sql/rls-*.sql` (policies hors migrations, `ENABLE` sans `FORCE`) ; `.env:9-10` (`neondb_owner`) ; `docs/RLS-ACTIVATION.md:5` (« policies inertes, l'app se connecte en owner qui bypasse la RLS ») | Rôle `app_rls` non-owner + `RLS_ENABLED=true` + `FORCE ROW LEVEL SECURITY` ; policies en migrations ; tests staging | **P2** |
| F-07 (SEC-45/47) | **Documents générés en Blob public + URL brute exposée aux apprenants** | 🟡 MOYENNE | ~5.3 | Conventions/attestations/certificats (identité, employeur, tarif) accessibles via une **URL publique permanente** sans auth ni scope tenant, si l'URL fuite (historique/partage/logs) | Moyenne (fuite d'URL, pas d'énumération : suffixe aléatoire) | `src/lib/blob.ts:19-24` (`access:"public"`) ; `src/lib/documents/publish.ts:50-61` ; `src/app/(app)/mes-documents/page.tsx:117`, `mes-certificats/page.tsx:99`. **NB** : les pièces CNI/RIB passent, elles, par un proxy authentifié (non exposées) | Blob `access:"private"` pour les documents sensibles + proxy authentifié (motif déjà en place côté B2B) | **P2** |
| F-08 (SEC-38) | **Lacunes du journal d'audit** (imputabilité) | 🟡 MOYENNE | ~4.3 | Suppression de compte tenant et authentification (login/logout) non traçables → réponse à incident et preuve Qualiopi/RGPD incomplètes | Faible (nécessite un accès légitime) | `src/lib/actions/comptes-actions.ts:99-104` (`supprimerCompte` sans AuditLog) ; `src/auth.ts:33-91` (login/logout non journalisés). Impersonation, elle, est tracée | `auditLog.create` sur `supprimerCompte` (3 branches) + events NextAuth LOGIN/LOGOUT | **P2** |
| F-09 (SEC-1b) | **Mots de passe en dur dans les scripts seed/ops** | 🟡 MOYENNE | ~5.3 | Compte admin à mot de passe connu du dépôt si non-roté (dont un **client réel** ASPR) | Conditionnelle (script exécuté sans rotation) | `prisma/seed.ts:12` (`?? "password123"`) ; `scripts/reset-aspr-admin.cjs:5` (`Aspr2026!`) ; `scripts/seed-qa.cjs:23`, `seed-test.cjs:38` | Router via `scripts/_seed-secret.cjs` (`seedPassword`) ; rotation du compte ASPR | **P2** |
| F-10 (SEC-11) | **2FA non imposée aux comptes à privilèges** | 🟡 MOYENNE | ~4.8 | ADMIN/SUPERADMIN opèrent sans second facteur par défaut ; un vol de mot de passe suffit | Dépend d'un vol de credential préalable | `src/lib/mandatory-2fa.ts:21` (`ENFORCE_ADMIN_2FA` OFF par défaut). TOTP correctement implémenté sinon | Activer `ENFORCE_ADMIN_2FA=true` en prod + codes de secours | **P2** |
| F-11 (SEC-54) | **`Math.random()` pour des mots de passe provisoires** (non-CSPRNG) | 🟡 MOYENNE | ~4.8 | Mot de passe provisoire apprenant/formateur prédictible | Moyenne (prédictibilité PRNG) | `src/components/candidats/candidat-access-panel.tsx:21` ; `src/components/formateur/formateur-admin-panels.tsx:85` ; `src/components/admin/account-forms.tsx:12` | Génération serveur via `crypto.randomBytes`/`randomUUID` ; + `eslint-plugin-security` | **P2** |
| F-12 (SEC-50b) | **Routes publiques `civique/lead` & `civique/checkout` sans rate-limit** | 🟡 MOYENNE | ~5.3 | Flood d'écritures Candidat (pollution CRM) + création massive de sessions Stripe (coût/spam) | Élevée (endpoints publics, aucune limite ni secret) | `src/app/api/civique/lead/route.ts:11-58` ; `src/app/api/civique/checkout/route.ts:33-104` | `checkLimit` par IP/email + captcha (aligner sur `/api/lead`) | **P2** |
| F-13 (SEC-48) | **Webhook Brevo faiblement authentifié** (secret en query, sans HMAC) | 🟡 MOYENNE | ~5.0 | Un tiers peut forger des events Brevo (statuts e-mail/désinscription) | Moyenne (secret fuit via logs/Referer ; comparaison non temps-constant) | `src/app/api/webhooks/brevo/route.ts:36-48` | Secret en **header** + `timingSafeEqual`, ou IP-allowlist Brevo | **P2** |
| F-14 (SEC-51) | **Opérations coûteuses sans plafond** (PDF `convention`, `civique/checkout/[id]`) | 🟡 MOYENNE | ~4.9 | Épuisement CPU/mémoire (1 rendu Chromium/candidat, tableau non borné) ; `civique/checkout/[id]` renvoie le `civicToken` sur simple `session_id` | Authentifiée (convention) / publique (checkout token) | `src/app/api/convention/route.ts:133-146` ; `src/app/api/civique/checkout/[id]/route.ts:31-49` | Plafonner le volume + `checkLimit` ; ne pas ré-exposer le token à volonté | **P2** |
| F-15 (SEC-53) | **Dépendances runtime vulnérables** (`axios`, `form-data`, `undici`, `image-size`) | 🟡 MOYENNE | — | CRLF injection, SSRF/prototype-pollution, DoS via libs transitives embarquées en prod | Variable selon la lib | `npm audit` : 32 vulns (1 critique + 18 hautes **cantonnées au dev/build** — Capacitor/shadcn/prisma-CLI) ; surface **runtime prod** = `axios`/`form-data`/`undici`/`image-size` (via `@turbodocx/html-to-docx`, `@vercel/blob`) | Montées transitives non-breaking ; puis planifier `firebase-admin`/Capacitor | **P2** |
| F-16 (SEC-76) | **`SECRETS_ENCRYPTION_KEY` possiblement absente en prod** | 🟡 MOYENNE | — | L'enregistrement d'une clé d'intégration tenant lève une exception tant que la variable n'est pas posée (fail-safe, mais bloquant) | Opérationnelle | `src/lib/crypto.ts:26-33` ; TODO connu (`todo_secrets_encryption_key`) | Définir la clé en prod Vercel | **P2** |
| F-17 (SEC-90) | **Composants à cycle rapide / pré-GA** (next-auth beta, next 16, chromium headless) | 🟡 MOYENNE | — | Fenêtre de CVE non patchées (Chromium = surface RCE si figé) | Veille | `package.json` (puppeteer ^25, @sparticuz/chromium ^149, firebase-admin ^14) | Dependabot/renovate + cadence MAJ mensuelle Chromium | **P3** |
| F-18 (SEC-58) | **Pas d'environnement de staging isolé** (le `.env` local pointe une base Neon prod/partagée) | 🟡 MOYENNE | — | Tests dynamiques/DAST impossibles sans risque prod ; risque d'opérations accidentelles sur données réelles | — | `.env:9-10` (DATABASE_URL prod) ; mémoire `ofmanager_prod_gotchas` | Provisionner un staging à base dédiée + secrets jetables | **P2** |
| F-19 (SEC-8) | Politique de mot de passe incohérente | 🟢 FAIBLE | ~3.7 | Comptes créés par invite/admin acceptent des mdp de 8 car. sans complexité | Faible | `src/lib/actions/admin-actions.ts:59` ; `entreprise-account-actions.ts:143` ; helper `password.ts:32` non branché partout | Centraliser `checkPasswordPolicy()` ; min 10-12 | **P3** |
| F-20 (SEC-87) | Jeton de reset dans le chemin d'URL + pas de self-reset staff | 🟢 FAIBLE | ~3.5 | Token capté par logs d'accès/Referer ; dépendance à un autre admin | Faible (token 192 bits, TTL 7j, usage unique) | `src/lib/actions/entreprise-account-actions.ts:19` | Token en POST/fragment + flux « mot de passe oublié » self-service | **P3** |
| F-21 (SEC-41) | `/api/upload` ouvert à tout rôle authentifié | 🟢 FAIBLE | ~3.1 | Un APPRENANT/ENTREPRISE peut stocker une image sur le domaine Blob de confiance (pas de XSS : images matricielles only) | Faible | `src/app/api/upload/route.ts:15-16` | Réserver au STAFF (`requireStaffTenant`) | **P3** |
| F-22 (SEC-33) | CSP `script-src 'unsafe-inline'` | 🟢 FAIBLE | ~4.0 | Amplifie l'impact d'une XSS (cf. F-02) | Faible | `next.config.ts:10` ; nonce prêt derrière flag `CSP_NONCE` OFF (`middleware.ts:16`) | Activer `CSP_NONCE=true` après vérif navigateur | **P3** |
| F-23 (SEC-68) | PII réglementaire non chiffrée au champ | 🟢 FAIBLE | ~3.1 | N° CNAPS/carte pro/SSIAP, date/lieu de naissance en clair (repose sur le chiffrement disque Neon) | Faible (nécessite accès base) | `src/lib/anonymise.ts:50-87`. Secrets tenant + TOTP, eux, chiffrés AES-256-GCM | (Optionnel) chiffrer les n° régaliens ; confirmer chiffrement at-rest Neon | **P3** |
| F-24 (SEC-75) | Mot de passe provisoire transmis en clair par e-mail | 🟢 FAIBLE | ~3.1 | Interception e-mail = credential jusqu'à 1re connexion | Faible (CSPRNG + changement forcé) | `src/lib/actions/parcours-actions.ts:862` | Lien tokenisé « définir mon mot de passe » (route existante) | **P3** |
| F-25 (SEC-28) | SSRF (défense en profondeur) | 🟢 FAIBLE | ~3.0 | `fetch` de relais d'URL stockées en base (Blob) sans allowlist d'hôte | Faible (URLs non arbitraires client) | `src/app/api/public/piece/[id]/route.ts:71` ; `download-response.ts:28` | Allowlister l'hôte Blob + bloquer IP privées/link-local | **P3** |
| F-26 (SEC-27) | Route handlers mutants sans jeton CSRF (SameSite seul) | 🟢 FAIBLE | ~3.1 | POST cross-site théorique sur `/api/upload` etc. | Faible (SameSite=Lax bloque le cross-site) | `src/app/api/upload/route.ts:14` | Contrôle `Origin`/`Sec-Fetch-Site` sur les route handlers mutants | **P3** |
| F-27 (SEC-26b) | Injection de contexte JSON-LD | 🟢 FAIBLE | ~4.6 | `</script>` dans un titre/terme (contenu tenant) casse le bloc script → XSS réfléchi public | Faible | `src/components/blog/blog-shell.tsx:120` ; `src/app/glossaire/[terme]/page.tsx:74` | Échapper `<`,`>`,`&` (`<`) avant injection | **P3** |
| F-28 (SEC-49) | Webhook Wedof sans anti-rejeu temporel | 🟢 FAIBLE | ~2.5 | Corps signé capturé rejouable (mais upsert idempotent) | Faible | `src/app/api/webhooks/wedof/[orgId]/route.ts` | Fenêtre temporelle / id d'event stocké | **P3** |
| F-29 (SEC-21) | Garde de route T3P en denylist (conception fragile) | 🔵 INFO | — | Aucune fuite (backstoppée par le middleware qui redirige ENTREPRISE) — **constat de fuite RÉFUTÉ** | Non exploitable | `src/app/api/parcours-t3p/[id]/route.ts:22` | Durcir en allowlist STAFF (ne pas dépendre du seul matcher) | **P3** |
| F-30 (SEC-78) | Pas de runbook de révocation de secret | 🔵 INFO | — | Délai/improvisation en cas de compromission | — | (absence) | Documenter rotation env + purge clés base | **P3** |
| F-31 (SEC-39/40) | Sentry DSN / alerting non actifs (config prod) | 🔵 INFO | — | Sans DSN prod : aucune capture d'erreur ni alerte sécu | — | `.env:37` (SENTRY_DSN vide) ; `sentry.server.config.ts` | Définir `SENTRY_DSN` + règles d'alerte | **P3** |
| F-32 (SEC-30) | Schémas Zod sans `.strict()` | 🔵 INFO | — | Champs en trop silencieusement retirés (strip par défaut) plutôt que rejetés | Non exploitable (strip effectif) | aucune occurrence `.strict()` dans `src` | `.strict()` sur les schémas dont `data` est spread dans Prisma | **P3** |

## Classement final
1. **CRITIQUE** — F-01 (impersonation).
2. **ÉLEVÉE** — F-02 (stored XSS BFLA), F-03 (révocation session), F-04 (rate-limit), F-05 (next-auth beta).
3. **MOYENNE** — F-06 à F-18.
4. **FAIBLE** — F-19 à F-28.
5. **INFORMATION** — F-29 à F-32.
